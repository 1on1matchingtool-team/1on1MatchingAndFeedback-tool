[pytest]
testpaths = backend/tests

markers =
    hard_constraints: tests that verify rules that must never be violated
    priority: tests that verify the matching priority order
    edge_cases: tests for boundary and scale scenarios
    data_integrity: tests that verify data is correctly updated after assignment